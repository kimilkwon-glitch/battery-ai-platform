# Battery Manager — 환경변수 예시 (실제 값은 .env.local에만 설정, 커밋 금지)

# 관리자 접근 키 (서버 전용 — NEXT_PUBLIC_ 접두어 사용 금지)
ADMIN_ACCESS_KEY=change-me-before-production

# (선택) 이전 11차 스크립트 호환 — ADMIN_ACCESS_KEY와 동일 값 권장
# ADMIN_API_SECRET=change-me-before-production
